package hub.guzio.JMatrix.setup;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.net.URI;
import java.util.Optional;

public record Namespaces(/*TODO*/) {
    public String serialize(){
        return "";
    }
}